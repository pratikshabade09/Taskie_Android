#!/usr/bin/env sh
# Taskie - Gradle Wrapper stub
# Replace with the real gradlew by running: gradle wrapper --gradle-version 8.4
GRADLE_HOME="${GRADLE_USER_HOME:-$HOME/.gradle}"
exec "$GRADLE_HOME/wrapper/dists/gradle-8.4-bin/*/gradle-8.4/bin/gradle" "$@"
